class MarketHash {
	constructor(pk_market_hash, market_hash, category) {
		this.pkMarketHash = pk_market_hash;
		this.marketHash = market_hash;
		this.category = category;
	}
}

export { MarketHash };
